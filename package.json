{
  "name": "software-testing-assignment",
  "version": "1.0.0",
  "description": "",
  "bin": {
    "coveralls": "./bin/coveralls.js"
  },
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "mocha -b -R spec",
    "test-coveralls": "npm run test && shx cat ./coverage/lcov.info | node ./bin/coveralls.js --verbose"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "chai": "^4.3.6",
    "mocha": "^10.1.0"
  },
  "devDependencies": {
    "coveralls": "^3.1.1",
    "mocha-lcov-reporter": "^1.3.0"
  }
}
